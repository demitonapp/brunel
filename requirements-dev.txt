# Checks. Installed into the same .venv so tests/run.sh can call them directly:
#
#     uv pip install -r requirements.txt -r requirements-dev.txt
#
# Pinned, like the toolchain: a linter that changes its mind between runs turns
# a green suite red for reasons that have nothing to do with the code.
#
# These are HARD requirements of tests/run.sh, not optional extras. A suite that
# silently skips its own static checks when a tool is missing is not a suite -
# the same argument requirements.txt makes about jsonschema and the fact gate.
ruff==0.14.2
mypy==1.18.2
